let newArray = ['Emilio', 32, true];
console.log(newArray[0])//output will say Emilio



//array is a special type of object
//holds multiple values called elements
//they are ordered and accessed using an index position
//the first index is 0
//store multiple related items


/*
Operations:
-integration
-sort
-filter
-transform

*/

//add elements using:
// push() - add elements to the end of the array
newArray.push('New Name');
console.log(newArray[newArray.length -1]);
// unshift() - add elements to the beginning of the array 
//newArray.unshift('Fanny');
//console.log(newArray);

//removing elements
//pop() - removes the last element
//shift() - removes the first element

//newArray.pop();
//console.log(newArray);

//newArray.shift();
//console.log(newArray);